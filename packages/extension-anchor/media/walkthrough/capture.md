### 选一段，然后按一下

打开任意代码文件，选中你要弄懂的那几行，按快捷键（默认 `Ctrl+Shift+A`，
可以在「开始」面板里看到你自己实际绑的那个键）：

[捕获选区并讲解](command:anchorExplain.capture)

### 只放了光标也行

只放光标不选内容时，会先问你「讲这一段，还是讲整个文件」——
整份文件常常是几百行，命中率通常比一段低，所以不替你猜。

### 讲什么由你确认

确认之后才会去请求模型。中途按 Esc 就是取消，什么也不会发生 ——
"点了一下就悄悄花掉一次调用"这种事不该出现。
