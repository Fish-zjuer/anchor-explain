# AGENTS.md — 工作协议

本项目采用「最小切片 + docs 唯一事实源」的推进方式。
任何 agent（包括未来的自己）在动任何文件之前，先读完本文件。

---

## 上下文纪律

每次会话只读 `docs/STATE.md` / `SLICES.md` / `DECISIONS.md` / `CONTRACTS.md`，不通读 `src/`；需要改哪个文件才读哪个。

## 文档结构

`docs/` 下固定六个文件：STATE / SLICES / DECISIONS / CONTRACTS / ARCHITECTURE / PRIOR-ART。
每个 package 一个 README 记职责与入口。

## 单一事实源

源码变了 docs 没同步 = 未完成；改 types 必须同步 `CONTRACTS.md`。

## 最小切片

一次只做一个切片；开工前在 `SLICES.md` 写清目标 / 范围 / 验收标准 / 回退点；用户说不符合就回退，不叠加。

## 禁区

- 用户未确认不进下一片
- 不改未声明文件
- 不让 docs 落后
- 不为理解通读 src
- 不重复读同一大文件超过两次

## 固定循环

读 STATE+SLICES → 声明切片 → 实现 → 能自动测就测，测不了明确说需用户实操 → 更新 docs → 按格式报告 → 等确认

## 报告格式

每次报告固定用这个格式：

    本次切片：S_N
    改了哪些文件：<列表>
    验收结果：<自动化 / 需用户实操>
    docs 已更新：STATE / SLICES / DECISIONS / CONTRACTS（哪些动了）
    下一步：<切片号 + 一句话>
    需要你确认：<能一句话回答的具体问题>

## 上下文友好写法

- 关键入口加 `// @anchor:` 注释
- 新增文件在 package README 补入口+职责
- 决策只追加不删除
- docs 不贴大段源码，只写签名/路径/语义

---

## 上下文与压缩纪律（上下文被压缩后仍要能准确续接）

> 这一节的存在原因：用户会定期压缩上下文。压缩之后，唯一可靠的东西是磁盘上的 docs。
> 因此 docs 必须足够厚，厚到"读完 docs 就能接着干活"，而不是"读完 docs 还得去翻源码"。

### 固定读取顺序（保持上下文前缀稳定 = 提高缓存命中）

    AGENTS.md
    → docs/STATE.md
    → docs/SLICES.md（只读当前切片那一节，不读整份历史）
    → 按需 docs/CONTRACTS.md（要动 types / 接口签名时）
    → 按需 docs/DECISIONS.md（要问"为什么这么做"时）

顺序和范围固定，不随意插入别的文件或调换次序。**不通读 src/。**

### 读文件预算（防止重复读拉低缓存命中）

- 一次会话内同一文件不读第二遍。确需回看：**先把结论写进 docs**，再只读该片段（带 `offset`/`limit`）
- 大文件（>500 行）先 Grep 定位，只读目标区间，禁止整文件读
- 开工前先在回复里声明"本切片将读取的文件清单"，之后不越界读别的文件
- 发现自己"需要读源码才能理解" → 这说明 **docs 缺失**，补 docs，不要靠读源码绕过

### docs 是唯一事实源

- src 改了 docs 没改 = 本切片未完成
- 改任何 type / 接口签名 → 必须同步 `CONTRACTS.md`（写 `path:line`）
- 换方案 / 砍做法 → 追加到 `DECISIONS.md`，**不删旧条目**
- docs 只写签名 / 路径 / 语义，不贴大段源码

### 收工必写

- `STATE.md`：当前切片、已完成、**下次第一件事（一句话，具体到文件）**
- `SLICES.md`：该切片标记完成 + `git tag slice-<编号>`
- 新增文件必须在对应 package 的 README 补"入口 + 职责"

### 续接自检（每次会话开始先答这四问）

1. 当前切片号与目标？
2. 上一片完成后的状态是什么（tag 名）？
3. 本切片要改哪些文件？
4. 验收靠自动化还是用户实操？

答不出来 → 回去读 docs，**而不是读 src**。

### 压缩后的恢复动作

若发现上下文里缺少细节且 docs 也没写清，按以下顺序处理，不要直接翻源码：

1. 把缺失点记进 `STATE.md` 的「待补 docs」字段
2. 用 Grep 精确定位，只读必要区间
3. **读完立刻把结论写进 docs**，再继续
